# TODO_FOR_USER

Things only the human can do. The agent enters items here instead of blocking
or simulating them. Please check off completed items, don't delete them — the
history is documentation.

Legend: 🔴 blocks a phase · 🟡 needed before phase completion · ⚪ nice to have

---

## Open

### ⚪ GitHub remote — done in Phase 0
- [x] Remote `origin` → `https://github.com/Elias02345/remote-agent.git`
      connected. `gh` was authenticated, initial push done.

### 🔴 Set `CCR_PUBLIC_DOMAIN` before pairing any device (blocks Phase 6 and 7)
- [ ] Set `CCR_PUBLIC_DOMAIN` in `server-provisioning/.env` to the domain/
      subdomain the owner login runs on permanently (e.g.
      `remote.your-domain.example`). This project ships no default —
      every installation picks its own. **A later change invalidates all
      registered passkeys**, so set it once, before the first pairing.
- [ ] Domain active in Cloudflare (nameservers pointing to Cloudflare).

### 🔴 Generate the owner credentials (blocks any device pairing)
The password and TOTP factors are unsatisfiable until these exist — deliberately,
so a half-configured daemon cannot pair anything. Generate them on the server:

```bash
claudecode-remoted --setup-owner --owner-email you@example.com
```

- [ ] Run it, then copy the three `CCR_OWNER_*` lines into
      `/etc/claudecode-remote/.env`. The password is read from stdin and never
      taken as a flag, because a flag value is visible to any local user via
      `ps` and lands in shell history.
- [ ] Add the printed `otpauth://` URI to your authenticator app. The secret is
      not shown again.
- [ ] Note that pairing still cannot **complete** until `CCR_PUBLIC_DOMAIN` is
      set — the passkey factor has no relying party to bind to otherwise.

### ⚪ Cloudflare API token — already handled
The owner runs CloudGate on a separate machine, and the Cloudflare token lives
there. Nothing in this repo needs it: `register-host.sh` talks to CloudGate's
own API with a CloudGate API key, and CloudGate talks to Cloudflare.

- [ ] Only needed if you ever set up a *second* CloudGate instance: a token
      with `Zone:DNS:Edit` + `Account:Cloudflare Tunnel:Edit`, stored in that
      instance's web UI. **Never in this repo.**
- [ ] Create a CloudGate API key (CloudGate web UI → Settings → API Keys) for
      `server-provisioning/cloudgate/register-host.sh` to use — export it as
      `CLOUDGATE_API_KEY`, never pass it as a script flag. **Not into the
      repo.**

### 🔴 Provide the target server (blocks verification of Phase 1–3)
- [ ] Set up an Arch Linux machine (bare metal or Proxmox VM, see D-02).
      **Confirmed by the owner: this machine does not exist yet** — this
      blocks only real-hardware verification, not development. Phases 1–3 are
      implemented and tested (via the Phase 1 container test harness, see
      `ROADMAP.md`) without the real server.
- [ ] Establish initial SSH access as root/first user so the provisioning
      script can be run once.
- [ ] Have your own machine's public `Ed25519` key ready — the script enters
      it for the `admin` user. Without the key you lock yourself out after
      the SSH hardening.

### ✅ Phase 4 visual Definition of Done — verified
Done against a live daemon rather than by eye, which turned out to be stronger:
the terminal's cell buffer was inspected directly.

- [x] Alternate screen: `bufferType` is `alternate` on attach (tmux's `?1049h`
      survived the passthrough), `vim` opened, and the shell prompt was restored
      after `:q!`.
- [x] Box drawing renders as real glyphs (`┌─┐`), not mojibake.
- [x] 256-colour and truecolor both arrive with the right cell attributes.
      **This found a bug:** truecolor was being quantised to the palette,
      because tmux needs the client declared RGB-capable. Fixed in
      `internal/terminal/tmux.go`.
- [x] Resize to 100×30 took effect through the WebSocket.
- [ ] Still worth doing once on the real machine: run Claude Code itself in a
      session. It is the actual target and the most demanding renderer.

### ✅ Antigravity CLI install command — resolved
- [x] It is **not** an npm package. It ships as a compiled binary with its own
      installer and the command is `agy`. `install-agent-stack.sh` now fetches
      the vendor installer (set `CCR_INSTALL_ANTIGRAVITY=0` to skip), and the
      shell alias points at `agy` rather than the nonexistent
      `antigravity-cli` that the architecture doc's example assumed.

### ✅ Android platform folder — created
- [x] `flutter create --platforms=android .` has been run, and the share-sheet
      `intent-filter` for `SEND` / `SEND_MULTIPLE` is in the manifest.
- [ ] The Kotlin side that hands an incoming share intent to Dart is still to
      be wired; `app/README.md` documents it. Needs an Android build to verify,
      which needs an Android SDK.

### 🟡 Register the server's GitHub key (before Phase 2 completion)
- [ ] The `Ed25519` public key generated by provisioning for the `agent` user
      must be entered under GitHub → Settings → SSH and GPG keys (account
      key, not a per-repo deploy key).

### 🟡 ntfy channel (before Phase 3 completion)
- [ ] Decide on either self-hosted ntfy or a topic on ntfy.sh and enter the
      URL in `.env` as `NTFY_URL`. The topic name must be hard to guess
      (ntfy.sh topics are public if you know the name).

### 🟡 Buy a FIDO2 hardware key (before Phase 9 completion)
- [ ] `YubiKey` or similar with USB **and** NFC — closes the Linux desktop
      passkey gap and simultaneously works as a roaming authenticator on
      Android.

### 🟡 Run `flutter create --platforms=android .` once (before an Android build)
- [ ] The Dart side of the Android features is written and tested, but there is
      no `app/android/` folder — a hand-written Gradle project would be
      confidently wrong. Run the command once in `app/`, then apply the manifest
      `intent-filter` and Kotlin snippet documented in `app/README.md`.

### ✅ Licence — MIT
- [x] Chosen on the owner's brief: permissive, compatible with everything, and
      not something that can get in the way later. Same licence as CloudGate,
      so code can move between the two projects freely.

### 🟡 Confirm where CloudGate reaches this machine
CloudGate runs on a separate server, so the host entry must point at an address
of *this* machine that CloudGate can route to — its Tailscale IP, normally.
`127.0.0.1` there is CloudGate's own loopback and reaches nothing.

- [ ] Set `CCR_BIND_ADDR` in `/etc/claudecode-remote/.env` to that same address
      and restart `claudecode-remoted`. Both halves have to agree.
- [ ] In CloudGate, the target scheme is **`http://`** — TLS terminates at
      Cloudflare's edge and the daemon holds no certificate.
- [ ] `register-host.sh` picks the Tailscale IP automatically when you omit the
      target, and warns loudly if it falls back to loopback.

### ⚪ restic repository password
- [ ] Generated by the installer in Phase 5. The human must back it up
      **outside the server** (password manager). Loss = loss of all backups.

### ✅ Bootstrap the GitHub wiki — done
The owner created the first page; all 12 pages are published and live at
<https://github.com/Elias02345/remote-agent/wiki>. Re-publish after edits with
`bash wiki/publish.sh`. Original note kept below for the record.

### ~~🟡 Bootstrap the GitHub wiki (blocks publishing `wiki/`)~~
Wikis are already enabled on the repo (`has_wiki: true`, set in Phase 0), but
GitHub does not create the backing `remote-agent.wiki.git` repository until the
first page is created through the web UI. There is no API for this and pushing
fails with `Repository not found` — both were tested in Phase 0.

- [ ] Open <https://github.com/Elias02345/remote-agent/wiki/_new>, create a page
      named `Home` with any content, and save it.
- [ ] Then run `bash wiki/publish.sh` from the repo root — it overwrites the
      bootstrap page with the versioned content from `wiki/`.

### ⚪ Review the English translation of the architecture doc
- [ ] Review the English translation of `docs/ARCHITECTURE.md` against
      `docs/ARCHITECTURE.de.md` — the German file is the original record.
