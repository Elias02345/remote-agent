<!--
  Two attributes on <application> below are security decisions, not defaults.

  allowBackup="false" / fullBackupContent="false": this app holds the device's
  Ed25519 private key. It lives in the Android Keystore, which is not backed up,
  but anything else the app persists would otherwise be copied into the user's
  cloud backup and restored onto a different phone. A paired device identity is
  exactly the thing that must not be restorable somewhere else.

  usesCleartextTraffic="false": the daemon speaks plaintext HTTP, but the mobile
  client reaches it through CloudGate over HTTPS, where TLS terminates at
  Cloudflare's edge. Direct plaintext to a Tailscale IP is a desktop and
  development path — inside WireGuard it is not actually exposed, but Android
  cannot express "only inside the tailnet", and a blanket cleartext permit to
  buy that would also permit it everywhere else. See app/README.md.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="claudecode_remote"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Share-sheet entry (architecture Section 8.3): "send this file to
                 ClaudeCode Remote" from any other app. The shared file is
                 uploaded into to-agent/ and then *offered* with an insert
                 button — it is never typed into the terminal, because the agent
                 may be mid-keystroke.

                 launchMode is singleTop above, so a share into a running app
                 delivers onNewIntent instead of building a second activity on
                 top of the live terminal. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="*/*"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
